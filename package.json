{
    "name": "puppeteer-ts-starter",
    "version": "0.0.1",
    "description": "A simple typescript template for a puppeteer project",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "ts-node ./src/index.ts",
        "dev": "nodemon --watch src/**/*.ts --exec npx ts-node src/index.ts"
    },
    "author": "kometa",
    "license": "MIT",
    "dependencies": {
        "nodemon": "^2.0.20",
        "puppeteer": "^13.4.1",
        "ts-node": "^10.7.0",
        "typescript": "^4.6.2"
    }
}
